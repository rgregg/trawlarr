/**
 * A placeholder, so `pnpm build` emits `packages/web/dist/index.html` and
 * `resolveWebRoot` has something to find. Task 10 replaces this file.
 */
export const App = (): JSX.Element => <h1>trawlarr</h1>;
